#include <GFX/BuiltInScene.hpp>
#include <raylib.h>

namespace Hotones {

void BuiltInScene::Update() {
    // if (IsKeyPressed(KEY_ESCAPE)) MarkFinished();
}

void BuiltInScene::Draw() {
    // ClearBackground(BLACK);
    // const char* msg = "Built-in pack";
    // int fs = 28;
    // DrawText(msg, 20, 20, fs, WHITE);
    // DrawText(m_name.c_str(), 20, 60, 22, WHITE);
    // DrawText("Press ESC to return.", 20, 110, 18, {180,180,180,255});
}

} // namespace Hotones
